# Environment variables — never commit these
.env
.env.local
.env.*.local

# Go build output
server/bin/
server/*.exe

# Node / frontend
client/node_modules/
client/dist/
client/.vite/

# Keep og-image.png (generated once, served as static asset)
!client/public/og-image.png

# OS
.DS_Store
Thumbs.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
